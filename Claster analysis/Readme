Кластеризация изображений цифр.

В этой программе происходит следующее:
1) Загружается 300 изображений из MNIST
2) Сжимается их размерность с 784 до 2D с помощью PCA
3) Применяется алгоритм кластеризации KMeans для разделения на 10 групп
4) Визуализируется результат — на графике каждая точка это изображение цифры, цвет которой соответствует кластеру, в который она попала

На графике вы увидите, как KMeans разделил цифры на кластеры. Поверх каждой точки отображается настоящая цифра, чтобы было видно, насколько точно сработала кластеризация.

Разработано в рамках учебного задания по предмету "Системы искусственного интеллекта".
